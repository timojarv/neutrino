import React from 'react';
import { Title } from '../base';

const EditTemplate = props => {
    return (
        <React.Fragment>
            <Title as="h2">Edit Template</Title>
        </React.Fragment>
    );
};

export default EditTemplate;
